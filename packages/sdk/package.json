{
  "name": "@precedence-dev/sdk",
  "version": "0.3.0",
  "description": "The client half of Precedence: installPrecedence collects, buffers, batches and delivers the events @precedence-dev/instrument bakes in, with an optional live plan overlay. Framework-agnostic, zero dependencies.",
  "license": "FSL-1.1-ALv2",
  "author": "Precedence",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/precedence-dev/sdk.git",
    "directory": "packages/sdk"
  },
  "homepage": "https://github.com/precedence-dev/sdk/tree/main/packages/sdk",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "prepare": "npm run build",
    "test": "npm run build && node test/invariants.mjs"
  }
}
