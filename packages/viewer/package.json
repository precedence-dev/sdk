{
  "name": "@precedence-dev/viewer",
  "version": "0.2.0",
  "description": "Serve Precedence's live picker (browser/agent.js) against a catalog.pcs and receive the exported event plan. Zero dependencies; driven by @precedence-dev/wizard.",
  "license": "FSL-1.1-ALv2",
  "author": "Precedence",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/precedence-dev/sdk.git",
    "directory": "packages/viewer"
  },
  "homepage": "https://github.com/precedence-dev/sdk/tree/main/packages/viewer",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "browser/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "prepare": "npm run build",
    "test": "npm run build && node test/invariants.mjs"
  }
}
