{
  "name": "precedence-sdk-repo",
  "version": "0.0.0",
  "private": true,
  "description": "Client-side Precedence packages: the runtime (@precedence/sdk) and the outcome picker (@precedence/viewer).",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
