{
  "name": "precedence-sdk-repo",
  "version": "0.0.0",
  "private": true,
  "description": "Client-side Precedence packages: the runtime (@precedence-dev/sdk) and the outcome picker (@precedence-dev/viewer).",
  "license": "FSL-1.1-ALv2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
